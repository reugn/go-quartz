language: go

go:
  - 1.12.x

env:
  - GO111MODULE=on

notifications:
  email: false

script:
  - go mod vendor && go test ./... -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)